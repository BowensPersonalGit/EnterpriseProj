@using EnterpriseProj.Entities
@using EnterpriseProj.Messages
@model List<Appointment>

@{
    ViewData["Title"] = "Admin Dashboard";
}

<h2>Admin Dashboard</h2>

<a class="btn btn-primary" href="https://localhost:7220/" target="_blank">Create New Appointment</a>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>Practictioner Name</th>
            <th>Speciality</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appointment in Model)
        {
            <tr>
                <td>@appointment.Title</td>
                <td>@appointment.Description</td>
                <td>@appointment.StartTime</td>
                <td>@appointment.EndTime</td>
                <td>@appointment.Practitioner.Name</td>
                <td>@appointment.Practitioner.Job</td>
                <td>
                    @if (appointment.isBooked)
                    {
                        <span>Booked</span>
                    }
                    else
                    {
                        <span>Unbooked</span>
                    }
                </td>
                <td>
                    @if (appointment.isBooked)
                    {
                        <a class="btn btn-danger" asp-controller="AppointmentAPI" asp-action="DeleteAppointmentAsync(@appointment.Id)">Cancel</a>
                    }
                    else
                    {
                        <a class="btn btn-primary"
                           asp-controller="Appointment"
                           asp-action="BookAdmin"
                           asp-route-appointmentId="@appointment.Id">
                            Book Appointment
                        </a>
                        //<a class="btn btn-primary" asp-controller="Appointment" asp-action="Book(@appointment.Id)">Book Appointment</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script>
        //confirms that they want to cancel the appointment and handles responses
        async function deleteappointment(id) {
            if (!confirm("are you sure you want to cancel this appointment?")) return;

            try {
                const response = await fetch(`/appointmentapi/cancel/${id}`, {
                    method: 'delete'
                });

                if (response.ok) {
                    alert("appointment cancelled.");
                    location.reload(); // reload page
                } else {
                    const error = await response.text();
                    alert("error cancelling appointment: " + error);
                }
            } catch (err) {
                console.error(err);
                alert("failed to cancel appointment.");
            }
        }
    </script>
}

<!--
    7 day outlook for all appointments


    they can book an appointment in the spa client
-->
